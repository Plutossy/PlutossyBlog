const state = {
  playList: [], //播放列表
  playContent: {},   //当前播放的歌曲信息
  current: ""  //当前歌曲的播放进度
}

const getters = {}

const mutations = {}

const actions = {}

export default {
  // 为当前模块开启命名空间
  namespaced: true,
  // 模块的 state 数据
  state,
  getters,
  mutations,
  actions
}